name: keep_ext_storage
description: Plugin to retrieve External paths in android
version: 0.0.1
homepage:

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  plugin:
    platforms:
      android:
        package: com.nikhil.flutter.keep_ext_storage
        pluginClass: KeepExtStoragePlugin